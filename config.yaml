# 不要集成在镜像中，应该用 ConfigMap 挂载到 /app 目录下
jwt_secret: "secret"

mysql:
  user: "root"
  password: "famcat777"
  # 这里需要修改你的数据库地址
  host: "host.docker.internal"
  port: "3306"
  database: "miniurl"

pgsql:
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "your_password"
  database: "your_database"
  sslmode: "disable"
  timezone: "Asia/Shanghai"

redis:
  host: "localhost"
  prot: "6379"
  password: "your_password"
  db: "0"